# 식수 관리 웹 서비스 MVP 명세

## 1. 문서 목적

이 문서는 식수 관리 웹 서비스의 첫 번째 개발 범위와 운영 규칙을 정의합니다. MVP는 직원용 페이지 없이 관리자가 당일 불참자를 체크하고, 오전 9시 50분 이후 최종 인원을 확정하여 식당으로 SMS를 보내는 데 집중합니다.

## 2. 배경과 목표

약 20명 규모의 회사에서 대부분의 직원은 거의 매일 식사에 참여합니다. 모든 직원에게 매일 응답을 요구하는 대신, 식사하지 않는 직원만 관리자에게 구두로 알리고 관리자가 시스템에 반영합니다.

MVP의 목표는 다음과 같습니다.

- 서버에 직접 접속하지 않고 직원 명단 관리
- 불참자만 선택하여 당일 식사 인원을 빠르게 계산
- 관리자 확인을 거친 SMS 발송
- 발송 결과와 과거 확정 기록 보존
- 주말과 공휴일의 잘못된 발송 방지
- 향후 자동 확정 및 발송으로 쉽게 전환할 수 있는 구조 확보

## 3. MVP 범위

### 포함

- 관리자 로그인
- 직원 등록 및 수정
- 직원의 재직 상태 관리
- 당일 불참자 선택
- 당일 식사 인원 자동 계산
- 오전 9시 50분 이후 관리자 최종 확정
- 확정과 동시에 식당으로 SMS 발송
- SMS 성공 및 실패 기록
- 실패한 SMS 수동 재전송
- 주말 자동 판별
- 대한민국 공휴일 자동 동기화 및 판별
- 확정 및 발송 이력 조회

### 제외

- 직원용 페이지
- 직원 로그인 및 인증
- 직원이 직접 식사 여부를 입력하는 기능
- 미래 날짜의 불참 예약
- 반차, 출장, 외근 등 별도 식사 상태
- 직원별 기본 식사 여부 설정
- 입사일 및 퇴사일 기반 자동 명단 반영
- 오전 9시 50분 자동 확정 및 자동 발송

## 4. 확정된 운영 정책

| 항목 | 정책 |
| --- | --- |
| 식사 상태 | `식사`, `불참` 두 가지 |
| 기본 상태 | 재직 직원 전원 `식사` |
| 불참 전달 | 직원이 관리자에게 구두 전달 |
| 입력 담당자 | 식수 관리자 1인 |
| 관리 날짜 | 오늘만 가능 |
| 집계 대상 | 상태가 `재직`인 직원만 포함 |
| 명단 관리 | 관리자가 직원 상태를 수동 관리 |
| 무입력 처리 | 재직 직원 전원이 식사하는 것으로 간주 |
| 확정 가능 시각 | 한국 시간 오전 9시 50분부터 |
| SMS 발송 | 관리자가 최종 확정하면 즉시 발송 |
| 확정 후 변경 | 불참자 및 식사 인원 수정 불가 |
| 주말·공휴일 | 확정 및 SMS 발송 제외 |
| 초기 발송 방식 | 수동 확정 및 발송 |
| 향후 발송 방식 | 안정화 후 오전 9시 50분 자동 발송 |

## 5. 핵심 업무 흐름

### 5.1 정상 영업일

1. 시스템이 오늘이 주말 또는 공휴일인지 확인합니다.
2. 관리자는 현재 재직 직원 명단을 확인합니다.
3. 구두로 전달받은 불참자를 체크합니다.
4. 시스템은 식사 인원을 실시간으로 계산합니다.
5. 오전 9시 50분 이후 관리자가 `최종 확정 및 문자 전송`을 누릅니다.
6. 시스템은 확정 전 확인 창에 재직자 수, 불참자 수, 식사 인원, 불참자 목록을 표시합니다.
7. 관리자가 확인하면 해당 날짜의 명단과 인원을 변경 불가능한 확정본으로 저장합니다.
8. 시스템은 확정본을 기준으로 SMS를 생성하여 식당에 전송합니다.
9. SMS 전송 결과를 저장하고 관리자에게 표시합니다.

### 5.2 관리자가 아무 작업도 하지 않은 경우

- 불참 기록이 없으므로 현재 재직 직원 전원이 식사하는 것으로 계산합니다.
- 자동 발송은 하지 않습니다.
- 관리자가 최종 확정 버튼을 눌러야 SMS가 발송됩니다.

### 5.3 SMS 발송 실패

- 식수 확정은 취소하지 않습니다.
- 확정된 인원과 문자 내용을 변경하지 않습니다.
- 실패 상태와 오류 메시지를 저장합니다.
- 관리자는 동일한 확정본으로 재전송할 수 있습니다.
- 재전송 시도 횟수와 각 결과를 기록합니다.

### 5.4 주말 또는 공휴일

- 당일 식수 입력과 최종 확정 버튼을 비활성화합니다.
- SMS를 발송하지 않습니다.
- 화면에 휴일 사유를 표시합니다.

## 6. 계산 규칙

```text
전체 인원 = 현재 상태가 '재직'인 직원 수
불참 인원 = 오늘 불참으로 체크된 재직 직원 수
식사 인원 = 전체 인원 - 불참 인원
```

휴직 또는 퇴사 상태의 직원은 화면의 당일 선택 대상과 식수 계산에서 제외합니다.

## 7. 화면 요구사항

### 7.1 관리자 로그인

- 고유 로그인 아이디와 비밀번호로 로그인
- 관리자 표시 이름은 로그인 아이디와 별도로 저장
- 비밀번호는 Argon2id 해시로 저장
- 로그인 성공 시 서버에 불투명 세션을 생성하고 HttpOnly 쿠키로 식별
- 인증되지 않은 사용자의 관리자 화면 접근 차단
- 로그아웃
- 관리자 공개 회원가입은 제공하지 않음

최초 관리자 계정은 배포 시 환경 변수로 생성합니다. 운영 환경에서는 HTTPS와 `Secure` 쿠키를 사용합니다.

### 7.2 오늘의 식수 관리

화면 상단에 다음 정보를 표시합니다.

- 오늘 날짜와 요일
- 정상 영업일 또는 휴일 상태
- 현재 시각과 확정 가능 시각
- 전체 재직자 수
- 불참 인원
- 최종 식사 인원
- 현재 상태: `입력 중`, `확정 처리 중`, `발송 성공`, `발송 실패`

직원 목록에는 다음 정보를 표시합니다.

- 직원 이름
- 부서(사용하는 경우)
- 불참 체크박스

필요한 조작은 다음과 같습니다.

- 이름 검색
- 불참 체크 및 해제
- 불참자만 모아보기
- 오전 9시 50분 이후 `최종 확정 및 문자 전송`
- 발송 실패 시 `동일 내용 재전송`

### 7.3 최종 확인 창

SMS 발송 전에 다음 내용을 다시 보여줍니다.

```text
재직자: 20명
불참자: 3명
식사 인원: 17명
불참자: 홍길동, 김철수, 이영희

확정 후에는 수정할 수 없으며 식당으로 문자가 전송됩니다.
```

### 7.4 직원 관리

- 직원 목록 조회
- 직원 추가
- 부서는 관리자가 정의한 목록에서 선택
- 이름, 연락처, 부서 등 기본 정보 수정
- 상태를 `재직`, `휴직`, `퇴사`로 변경
- 직원 삭제 버튼 제공
- 삭제 시 DB에서 물리적으로 제거하지 않고 목록과 집계에서 숨김

입사일과 퇴사일은 식수 대상 자동 판별에 사용하지 않습니다.

### 7.5 발송 이력

- 날짜별 최종 인원 조회
- 당시 재직자 수와 불참자 목록 조회
- 수신 번호와 문자 내용 조회
- 성공, 실패 등 발송 상태 조회
- 발송 및 재전송 시각 조회
- 실패 사유 조회

## 8. 시간과 확정 규칙

- 모든 날짜와 시간은 `Asia/Seoul`을 기준으로 처리합니다.
- 오전 9시 50분 전에는 최종 확정 버튼을 비활성화합니다.
- 오전 9시 50분 이후 최종 확정 전까지는 불참자를 변경할 수 있습니다.
- 최종 확정 시점부터 해당 날짜의 식수 데이터는 수정할 수 없습니다.
- 확정 요청이 동시에 여러 번 들어와도 최초 한 건만 성공해야 합니다.
- 확정 도중 화면을 새로고침해도 중복 SMS가 발송되어서는 안 됩니다.

## 9. 휴일 판별

### 주말

서버가 오늘의 요일을 확인하여 토요일과 일요일을 자동으로 제외합니다.

### 법정공휴일과 대체공휴일

공공데이터포털의 한국천문연구원 특일 정보 API에서 대한민국 공휴일을 가져옵니다.

- 연도 단위로 공휴일을 동기화하여 DB에 저장
- 서비스 시작 시 현재 연도 데이터 확인
- 다음 연도 데이터 사전 동기화
- 정기적인 재동기화로 임시공휴일 등 변경 반영
- 외부 API 장애 시 마지막으로 저장한 데이터 사용
- 동기화 성공 및 실패 시각 기록

회사 자체 휴무일을 자동으로 알 수 있는 외부 데이터는 없으므로, 이 기능이 필요해지면 별도 예외 휴일 등록 기능을 추가합니다.

## 10. SMS 정책

### 기본 문구

```text
[회사명] 2026년 9월 8일 식사 인원은 17명입니다.
```

- 불참자 이름은 SMS에 포함하지 않습니다.
- 발송 당시의 전화번호와 문자 내용을 기록으로 보존합니다.
- 같은 날짜의 최초 확정 SMS는 한 번만 발송합니다.
- 재전송은 실패한 확정본에 대해서만 명시적인 관리자 조작으로 실행합니다.

SMS 업체, 발신 번호, 수신 번호 관리 방식, 최종 문구는 구현 전에 확정해야 합니다.

## 11. 데이터 모델 초안

### admins

| 필드 | 설명 |
| --- | --- |
| `id` | 관리자 ID |
| `login_id` | 고유 로그인 아이디 |
| `display_name` | 화면과 확정 기록에 표시할 이름 |
| `password_hash` | Argon2id 비밀번호 해시 |
| `is_active` | 계정 활성 여부 |
| `last_login_at` | 마지막 로그인 시각 |
| `created_at` | 생성 시각 |

### admin_sessions

| 필드 | 설명 |
| --- | --- |
| `id` | 세션 ID |
| `token_hash` | 불투명 세션 토큰의 해시 |
| `csrf_token` | 요청 위조 방지 토큰 |
| `admin_id` | 관리자 ID |
| `expires_at` | 만료 시각 |
| `created_at` | 생성 시각 |

### employees

| 필드 | 설명 |
| --- | --- |
| `id` | 직원 ID |
| `name` | 직원 이름 |
| `phone` | 연락처(선택) |
| `department` | 부서(선택) |
| `status` | `ACTIVE`, `LEAVE`, `RETIRED` |
| `created_at` | 등록 시각 |
| `updated_at` | 수정 시각 |

### daily_meals

날짜별 최종 확정 상태와 집계 결과를 저장합니다.

| 필드 | 설명 |
| --- | --- |
| `id` | 식수 ID |
| `meal_date` | 식사 날짜, 유일값 |
| `status` | `DRAFT`, `CONFIRMED` |
| `active_count` | 확정 당시 재직자 수 |
| `absent_count` | 확정 당시 불참자 수 |
| `meal_count` | 확정 당시 식사 인원 |
| `confirmed_by` | 확정 관리자 |
| `confirmed_at` | 확정 시각 |
| `created_at` | 생성 시각 |
| `updated_at` | 수정 시각 |

### meal_absences

불참자만 저장합니다.

| 필드 | 설명 |
| --- | --- |
| `id` | 불참 기록 ID |
| `daily_meal_id` | 날짜별 식수 ID |
| `employee_id` | 직원 ID |
| `employee_name_snapshot` | 확정 당시 직원 이름 |
| `created_at` | 등록 시각 |

`daily_meal_id`와 `employee_id` 조합에 유일성 제약을 둡니다.

### sms_logs

| 필드 | 설명 |
| --- | --- |
| `id` | SMS 기록 ID |
| `daily_meal_id` | 확정 식수 ID |
| `attempt_number` | 발송 시도 번호 |
| `recipient` | 식당 수신 번호 |
| `message` | 실제 발송 문자 |
| `status` | `PENDING`, `SENDING`, `SUCCESS`, `FAILED` |
| `provider_message_id` | SMS 업체 메시지 ID |
| `sent_at` | 발송 시각 |
| `error_message` | 실패 사유 |
| `created_at` | 생성 시각 |

### holidays

| 필드 | 설명 |
| --- | --- |
| `id` | 휴일 ID |
| `holiday_date` | 휴일 날짜, 유일값 |
| `name` | 공휴일 명칭 |
| `source` | 공휴일 데이터 출처 |
| `synced_at` | 동기화 시각 |

## 12. 핵심 안전장치

- 확정 처리와 당일 확정본 생성은 DB 트랜잭션으로 처리
- 날짜별 확정 레코드에 유일성 제약 적용
- 중복 버튼 클릭과 동시 요청 방지
- SMS 요청마다 중복 방지 키 사용
- SMS API 키와 DB 비밀번호를 환경 변수 또는 비밀 저장소에서 관리
- 관리자 권한이 없는 사용자의 직원 정보 및 발송 기록 접근 차단
- 모든 외부 접속에 HTTPS 사용
- 확정본과 SMS 발송 이력 보존
- DB 정기 백업

## 13. 구현 원칙

최종 확정과 SMS 발송 로직은 화면 버튼에 종속시키지 않고 공통 애플리케이션 서비스로 구현합니다.

```text
MVP
관리자 버튼 ──→ 공통 확정·발송 서비스

향후
09:50 스케줄러 ──→ 동일한 공통 확정·발송 서비스
```

이를 통해 자동 발송 전환 시 계산과 발송 로직을 다시 작성하지 않고 실행 주체만 변경할 수 있습니다.

## 14. MVP 완료 조건

- 관리자가 직원과 재직 상태를 관리할 수 있다.
- 오늘의 재직 직원 중 불참자를 선택할 수 있다.
- 불참자를 선택하지 않으면 전원 식사로 정확히 계산된다.
- 휴직 및 퇴사 직원은 계산에서 제외된다.
- 오전 9시 50분 전에는 확정할 수 없다.
- 주말과 공휴일에는 확정 및 SMS 발송을 할 수 없다.
- 최종 확인 후 식수 기록이 확정되고 SMS가 한 번 발송된다.
- 확정 후 당일 인원과 불참자를 변경할 수 없다.
- 중복 요청에도 같은 날짜의 최초 SMS가 중복 발송되지 않는다.
- 발송 결과와 오류를 관리자가 확인할 수 있다.
- 실패한 SMS를 동일한 확정 내용으로 재전송할 수 있다.
- 과거 확정 당시의 인원, 불참자, 문자 내용이 보존된다.

## 15. 후속 개발

MVP 운영이 안정화되면 다음 기능을 순차적으로 추가합니다.

1. SMS 실패 자동 재시도
2. 지속적인 실패 시 관리자 알림
3. 감사 로그와 운영 모니터링
4. DB 자동 백업 검증
5. 오전 9시 50분 자동 확정 및 발송
6. 필요 시 회사 자체 휴무일 예외 관리

자동 발송 단계에서는 관리자 버튼 대신 스케줄러가 공통 확정·발송 서비스를 호출하며, 기존 중복 방지와 확정본 보존 정책을 그대로 적용합니다.

## 16. 구현 전 남은 결정 사항

- SMS 업체
- SMS 발신 번호
- 식당 수신 번호의 저장 및 변경 방식
- 최종 SMS 문구
- 수동 재전송 허용 조건과 최대 횟수
- 지속적인 실패 시 관리자 알림 채널
- 데이터 및 발송 이력 보존 기간
